document.addEventListener("DOMContentLoaded", () => {
    const button = document.getElementById("clickBtn");

    button.addEventListener("click", () => {
        alert("Hello! Your GitHub Pages website script is working properly.");
    });
});
